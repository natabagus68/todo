import { createSlice, createAsyncThunk } from '@reduxjs/toolkit'

export const dataSlice = createSlice({
    name: 'data',
    initialState:{
        data: [],
        done:[]
    },reducers:{

    },
    extraReducers:{
        
    }
})